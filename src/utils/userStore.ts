// Legacy module retained for compatibility.
// This codebase now uses the backend via `useServices()` and `src/lib/api.ts`.
// If you still have older UI that imports `UserStore`, migrate it to `useServices()`.
export {};
